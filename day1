
The down below C++ code finds the missing number from the array that should be present in between the array

#include<iostream> 
using namespace std; 


	
/* getMissingNum takes array and 
size of array as arguments*/
getMissingNum(int arr[],int n) 
{ 
	int i, sum; 
	sum = (n + 1) * (n + 2) / 2; 
	for ( i = 0; i< n; i++) 
		sum -= a[i]; 
	return sum; 
} 



int main() 
{ 
	 
	int arr[] = {1, 2, 4, 5, 6}; 
	int missing =getMissingNum(a, 5); 
	cout << "the missing number from array is"<<missing; 
} 
